<%= render 'admin/user_syncs/loader' %>

<h6 class="text-muted mb-3">User Syncs</h6>

<h1>Syncs for <%= @user.name %></h1>

<%= render partial: 'admin/user_syncs/syncs' %>

<div class="mt-3">
  <%= link_to 'Back to User', admin_user_path(@user), class: 'btn btn-secondary' %>
</div>
